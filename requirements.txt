pywebview>=5.0
shapely>=2.0
# Native desktop window (Qt / QtWebEngine) — the stable app host:
PySide6>=6.6
# Geometry helpers (also loaded by the in-browser Pyodide engine):
numpy>=1.26
# Live MAVLink link to the drone (direct mission upload + telemetry over
# cable COM / UDP / TCP). pyserial is required for serial (cable/radio) ports.
pymavlink>=2.4
pyserial>=3.5
