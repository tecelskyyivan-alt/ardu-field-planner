<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDevelopmentRegion</key>
  <string>uk</string>
  <key>CFBundleDisplayName</key>
  <string>Field Mission Planner</string>
  <key>CFBundleName</key>
  <string>FMP</string>
  <key>CFBundleExecutable</key>
  <string>$(EXECUTABLE_NAME)</string>
  <key>CFBundleIdentifier</key>
  <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
  <key>CFBundlePackageType</key>
  <string>APPL</string>
  <key>CFBundleShortVersionString</key>
  <string>$(MARKETING_VERSION)</string>
  <key>CFBundleVersion</key>
  <string>$(CURRENT_PROJECT_VERSION)</string>
  <key>LSRequiresIPhoneOS</key>
  <true/>

  <!-- Live "my location" tracking in flight + map centering. iOS 15+ WKWebView
       serves navigator.geolocation only when the app itself is authorised. -->
  <key>NSLocationWhenInUseUsageDescription</key>
  <string>Показує ваше місце на карті під час планування й польоту.</string>

  <!-- REQUIRED (iOS 14+) to reach the drone's WiFi / ELRS backpack on the local
       network over UDP. Without this the OS silently blocks the telemetry link. -->
  <key>NSLocalNetworkUsageDescription</key>
  <string>Зв'язок із дроном (телеметрія MAVLink) через WiFi-міст / ELRS-бекпак у локальній мережі.</string>

  <!-- Allow the in-app http://127.0.0.1 server (serves the offline web app). -->
  <key>NSAppTransportSecurity</key>
  <dict>
    <key>NSAllowsLocalNetworking</key>
    <true/>
  </dict>

  <!-- Simple system launch screen (no storyboard file needed, iOS 14+). -->
  <key>UILaunchScreen</key>
  <dict/>

  <key>UISupportedInterfaceOrientations</key>
  <array>
    <string>UIInterfaceOrientationPortrait</string>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
  </array>
  <key>UISupportedInterfaceOrientations~ipad</key>
  <array>
    <string>UIInterfaceOrientationPortrait</string>
    <string>UIInterfaceOrientationPortraitUpsideDown</string>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
  </array>
  <key>UIRequiresFullScreen</key>
  <true/>
</dict>
</plist>
